{
  "name": "@adblockify/webext",
  "version": "0.1.0",
  "description": "Stop annoying ads & pop-ups compromising your online privacy",
  "author": "AdBlockify <support@adblockify.com>",
  "license": "GPLV3",
  "scripts": {
    "generate": "npx tailwindcss -i ./tailwind.css -o ./platform/mv3/extension/css/tailwind.css",
    "build": "rm -rf dist && npm run generate && make mv3-chromium",
    "release": "npm run generate && make mv3-full"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
